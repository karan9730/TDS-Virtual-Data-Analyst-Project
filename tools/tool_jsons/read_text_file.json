{
  "type": "function",
  "function": {
    "name": "read_text_file",
    "description": "Reads a plain text file from a given directory and returns its content.",
    "parameters": {
      "type": "object",
      "properties": {
        "file_name": {
          "type": "string",
          "description": "The name of the file to read (e.g., questions.txt). Do NOT include directory paths."
        },
        "directory": {
          "type": "string",
          "enum": ["uploads", "outputs"],
          "description": "The directory to read from. Must be either 'uploads' or 'outputs'."
        }
      },
      "required": ["file_name", "directory"],
      "additionalProperties": false
    },
    "strict": true
  }
}