{
  "type": "function",
  "function": {
    "name": "save_to_json",
    "description": "Saves a stringified JSON object to a .json file in the outputs directory.",
    "parameters": {
      "type": "object",
      "properties": {
        "json_string": {
          "type": "string",
          "description": "A valid JSON string to be saved to file."
        },
        "file_name": {
          "type": "string",
          "description": "Filename to save the JSON as (must end with .json)."
        }
      },
      "required": ["json_string", "file_name"],
      "additionalProperties": false
    },
    "strict": true
  }
}