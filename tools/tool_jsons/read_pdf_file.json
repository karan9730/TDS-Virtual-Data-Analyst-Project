{
  "type": "function",
  "function": {
    "name": "read_pdf_file",
    "description": "Reads a PDF file from a given directory and returns its text content.",
    "parameters": {
      "type": "object",
      "properties": {
        "file_name": {
          "type": "string",
          "description": "The name of the PDF file to read (e.g., example.pdf). Do NOT include directory paths."
        },
        "directory": {
          "type": "string",
          "enum": ["uploads", "outputs"],
          "description": "The directory to read from. Must be either 'uploads' or 'outputs'."
        }
      },
      "required": ["file_name", "directory"],
      "additionalProperties": false
    },
    "strict": true
  }
}